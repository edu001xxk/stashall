name: Cloudflare 优选套件
desc: 包含首页磁贴 + 自动生成优选节点

# ================================
# 1. 首页磁贴 (UI 显示)
# ================================
tiles:
  - name: CF-Best-IP
    title: Cloudflare 优选
    content: 点击运行测速
    icon: network
    collapsed: false

script-providers:
  CF-Best-IP:
    # 🔗 这里填 cf.js 的链接 (用于测速和存IP)
    url: "https://raw.githubusercontent.com/你的用户名/仓库/分支/cf.js?v=final"
    interval: 0

# ================================
# 2. 自动节点 (Proxy 生成)
# ================================
proxy-providers:
  "🚀 自动优选":
    type: script
    # 🔗 这里填 cf-node.js 的链接 (用于读取IP并生成节点)
    url: "https://raw.githubusercontent.com/你的用户名/仓库/分支/cf-node.js?v=final"
    interval: 300
    health-check:
      enable: true
      url: http://www.gstatic.com/generate_204
      interval: 300

# ================================
# 3. (可选) 自动把节点加入策略组
# ================================
# 如果你想让这个节点自动出现在某个组里，可以加下面这段
# 如果不加，它会作为一个单独的“代理集”出现在列表底部，也能用
# proxy-groups:
#   - name: "CF自动组"
#     type: select
#     use:
#       - "🚀 自动优选"

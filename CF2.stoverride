name: Cloudflare 优选 (加速版)
desc: 使用国内加速链接，解决节点加载失败问题

# 1. 首页磁贴 (UI)
tiles:
  - name: CF-Best-IP
    title: Cloudflare 优选
    content: 点击运行测速
    icon: network
    collapsed: false

script-providers:
  CF-Best-IP:
    # 这里的 cf.js 也加上加速，防止 UI 加载不出来
    url: "https://mirror.ghproxy.com/https://raw.githubusercontent.com/edu001xxk/stashall/refs/heads/main/cf.js?v=ui_fast_01"
    interval: 0

# 2. 节点生成 (核心修复)
proxy-providers:
  "CF-Provider":
    type: script
    # ⚠️ 关键修改：使用了镜像加速链接，解决国内下载失败问题
    url: "https://mirror.ghproxy.com/https://raw.githubusercontent.com/edu001xxk/stashall/refs/heads/main/cf-node.js?v=node_fast_01"
    interval: 300
    health-check:
      enable: true
      url: http://www.gstatic.com/generate_204
      interval: 300

# 3. 策略组
proxy-groups:
  - name: "🚀 自动优选"
    type: select
    use:
      - "CF-Provider"
